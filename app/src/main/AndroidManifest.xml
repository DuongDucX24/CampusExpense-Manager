<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
        tools:targetApi="31">
        <activity android:name=".activities.MenuActivity"/>
        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.ForgotPasswordActivity"/>
        <activity android:name=".activities.RecurringExpenseActivity"/>
        <activity android:name=".activities.AddRecurringExpenseActivity"/>
        <activity android:name=".activities.ExpenseOverviewActivity"/>
        <service android:name=".service.RecurringExpenseService" />
        <service android:name=".service.NotificationService" />
        <activity android:name=".activities.AddBudgetActivity"/>
        <activity android:name=".activities.EditBudgetActivity"/>
        <activity android:name=".activities.AddExpenseActivity"/>
        <activity android:name=".activities.EditExpenseActivity"/>
        <activity android:name=".activities.AllExpensesActivity"/>
        <activity android:name=".activities.AllBudgetsActivity"/>
        <activity android:name=".activities.ImportActivity"/>
        <activity android:name=".activities.ExportActivity"/>
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>